Título: FASE 2: respuestas 0521, 0083, 0579, 0104

Descripción:

Entrega de la FASE 2 que incluye:
- Implementaciones en `myanswers/answer_0521.py`, `answer_0083.py`, `answer_0579.py`, `answer_0104.py`.
- Validador automático `myanswers/run_validation.py` y casos en `myanswers/cases/`.
- Registro de validación en `myanswers/diagnostico.txt` (todas OK con Python 3.14).
- Instrucciones de reproducción en `myanswers/FASE2_entrega.txt` y dependencias en `requirements.txt`.

Instrucciones para el revisor:
1. Crear y activar virtualenv con Python 3.14 (o usar conda):

```
py -3.14 -m venv .venv_fase2
.\.venv_fase2\Scripts\Activate.ps1
python -m pip install --upgrade pip wheel setuptools
python -m pip install --only-binary=:all: -r requirements.txt
```

2. Ejecutar validador:

```
python -m myanswers.run_validation
```

Notas:
- La entrega esta en `main`.
- Si necesitan cambios menores, indíquenmelos en el PR.
